// globalConfig.mjs
// this runs right when you import it (side effect)

console.log("global config: setting stuff up... (fake)");

export const CONFIG_VERSION = "0.1"; // not really used but could be later
